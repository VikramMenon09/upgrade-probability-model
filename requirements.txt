# Core model (model.py) needs ONLY the Python standard library.
# The packages below are for the notebook, the generated charts and the app.

pandas>=1.3
matplotlib>=3.4

# Optional — only needed to run the interactive Streamlit app (app.py):
streamlit>=1.20

# Optional — only needed for the in-notebook interactive sliders:
ipywidgets>=7.6
